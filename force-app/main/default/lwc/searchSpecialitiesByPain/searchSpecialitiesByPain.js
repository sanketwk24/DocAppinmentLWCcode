import { LightningElement } from 'lwc';

export default class SearchSpecialitiesByPain extends LightningElement {

    handleChange(){

    }

    handleClick(event){
        return event.target.value;
    }
}